<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_building"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.nirupama.prasad.mapmysjsu.BuildingActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/map_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorToolBar"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        android:subtitleTextColor="#FFFFFF"
        app:titleTextColor="#FFFFFF"/>

    <TextView
        android:text="Building Details: "
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/map_toolbar"
        android:layout_alignParentStart="true"
        android:layout_marginStart="6dp"
        android:id="@+id/activity_building_textView"
        android:layout_alignParentEnd="true" />

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:srcCompat="@drawable/bbc"
        android:layout_below="@+id/activity_building_textView"
        android:layout_alignStart="@+id/activity_building_textView"
        android:id="@+id/activity_building_imageView" />

</RelativeLayout>
